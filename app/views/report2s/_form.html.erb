<%= form_for(@report2) do |f| %>
  <% if @report2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@report2.errors.count, "error") %> prohibited this report2 from being saved:</h2>

      <ul>
      <% @report2.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start_date %><br />
    <%= f.date_select :start_date %>
  </div>
  <div class="field">
    <%= f.label :end_date %><br />
    <%= f.date_select :end_date %>
  </div>
  <div class="field">
    <%= f.label :graph_type %><br />
    <%= f.text_field :graph_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
